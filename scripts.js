$(document).ready(function() {
  $("form#two-numbers").submit(function(event) {
    event.preventDefault();
    var toCount = parseInt($("input#number-to").val());
    var byCount = parseInt($("input#number-by").val());
        alert(toCount + "," + byCount);
        alert(toCount * byCount);
    var results = toCount * byCount);
      for (var index= 0; index <= toCount; index+= byCount {
        alert(results);

      }
    });



  });
